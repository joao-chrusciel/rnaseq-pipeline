nextflow.enable.dsl = 2

includeConfig 'conf/base.config'
includeConfig 'conf/defaults.config'

params {
    input = null
    mirge_lib = null
    species = null
    outdir = 'results'
}

profiles {
    docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g)'
    }
    apptainer {
        apptainer.enabled = true
        apptainer.autoMounts = true
        process.container = "${projectDir}/images/rnaseq.sif"
    }
    conda {
        conda.enabled = true
    }
    slurm {
        process.executor = 'slurm'
        process.queue = 'short'
    }
    lpc {
        // local computer (personal pc)
        // uses smaller Kraken2 DB that fits in limited RAM (~8 GB)
        params.kraken2_db = "/home/joao/bio_projects/references/kraken2_db"
        params.kraken2_memory_mapping = true
        params.pirna_index_dir = "/home/joao/bio_projects/references/pirbase_v3"
        params.rnacentral_db_dir = "/home/joao/bio_projects/references/rnacentral"
    }
    hpc {
        // HPC environment
        // uses full PlusPFP DB (100 GB more RAM)
        params.kraken2_db = "/home/joaochrusciel/bio_projects/references/kraken2_pluspf_20260226"
        params.kraken2_memory_mapping = false
        params.pirna_index_dir = "/home/joaochrusciel/bio_projects/references/pirbase_v3"
        params.rnacentral_db_dir = "/home/joaochrusciel/bio_projects/references/rnacentral"
        process.executor = 'slurm'
        process.queue = 'short'
    }
    test {
        includeConfig 'conf/test.config'
    }
}